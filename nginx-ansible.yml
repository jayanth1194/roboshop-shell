- name: nginx_install and run it 
  hosts: nginx
  become: yes
  tasks:
  - name: installing_nginx
    ansible.builtin.yum:
      name: nginx
      state: installed 
  - name: start nginx
    ansible.builtin.service:
      name: nginx
      state: started


