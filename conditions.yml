
- name: check user exits
  hosts: localhost
  tasks: 
  - name: conditions check 
    ansible.builtin.command: id roboshop
    register: output1
    ignore-errors: true
  - name: check output 
    ansible.builtin.debug:
      msg: " print {{output1}}"

  